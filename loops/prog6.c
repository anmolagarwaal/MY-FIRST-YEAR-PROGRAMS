#include <stdio.h>
int main()
{
  // prime or not

  printf("enter the number you want to check if its prime or not ");
    int n;
    scanf("%d",&n);

    for(int i=1; i<n-1; i++ )
    {
        if(n%i==0 )
        {

        }
    }
}
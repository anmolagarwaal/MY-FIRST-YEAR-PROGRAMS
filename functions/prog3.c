#include <stdio.h>
#include <math.h>
int main(
)
{
  // library fucn
  
  int a;
    printf("enter a number");
    scanf("%d",&a);

     int root= sqrt(a);
     printf("sqr root is %d",root);
}